const ItemListContainer = props => {
  return (
    <div className="w-full flex justify-center items-center">
      <h1 className="text-3xl p-20">{props.title}</h1>
    </div>
  );
};

export default ItemListContainer;
